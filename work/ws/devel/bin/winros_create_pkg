#!/usr/bin/env python
# creates a relay to a python script source file, acting as that file.
# The purpose is that of a symlink
with open("C:/work/ws/src/win_ros/msvc/winros_create/winros_create_pkg/scripts/winros_create_pkg", 'r') as fh:
    exec(fh.read())
